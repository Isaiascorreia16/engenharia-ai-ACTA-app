package cv.acta.app

import org.junit.Assert.assertEquals
import org.junit.Test

/** Teste trivial da Fase 0: garante que o passo de testes do workflow tem algo para executar. */
class ArranqueTest {
    @Test
    fun aritmeticaBasica() {
        assertEquals(4, 2 + 2)
    }
}
